{{ form_start(form) }}
    <h4 class="font-semibold text-lg md:text-xl mb-6">Datos del perro</h4>
    <div class="grid grid-cols-1 sm:grid-cols-2 gap-x-8">
        {{ form_row(form.name) }}
        {{ form_row(form.breed) }}
        {{ form_row(form.color) }}
        {{ form_row(form.birthdate) }}
    </div>
    {{ form_row(form.notes) }}
            {{ form_row(form.imageFile) }}
            {{ form_row(form.active) }}

    <h4 class="font-semibold text-lg md:text-xl mt-8 mb-6">Datos de facturación / dueñ@</h4>
    <div class="grid grid-cols-1 sm:grid-cols-2 gap-x-8">
        {{ form_row(form.ownerName) }}
        {{ form_row(form.ownerIdentification) }}
        {{ form_row(form.ownerPhone) }}
        {{ form_row(form.ownerEmail) }}
        {{ form_row(form.ownerAddress) }}
        {{ form_row(form.ownerCP) }}
        {{ form_row(form.ownerLocation) }}
        {{ form_row(form.ownerProvince) }}
    </div>

    <div class="mt-8 md:mt-12 flex items-center justify-end">
        <a href="{{ path('admin_dog') }}" class="px-6 py-2.5 bg-transparent font-medium text-xs leading-tight uppercase hover:text-sky-700 focus:text-sky-700 focus:outline-none focus:ring-0 transition duration-150 ease-in-out">Cancelar</a>
        <button class="px-6 py-2.5 bg-sky-500 text-white font-medium text-xs leading-tight uppercase rounded shadow-md hover:bg-sky-600 hover:shadow-lg focus:bg-sky-600 focus:shadow-lg focus:outline-none focus:ring-0 active:bg-sky-700 active:shadow-lg transition duration-150 ease-in-out">{{ button_label|default('Guardar cliente') }}</button>
    </div>
{{ form_end(form) }}