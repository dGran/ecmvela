{{ form_start(form) }}
<div class="p-6 text-sm">
    <div class="grid grid-cols-1 sm:grid-cols-2 gap-x-8">
        {{ form_row(form.date) }}
{#        <div class="mb-4 flex flex-col group">#}
{#            <label class="mb-1 text-[12px] font-medium text-slate-600 uppercase tracking-wide required" for="booking_time">Hora</label>#}
{#            <select name="booking[date]" id="booking_date">#}
{#                {% for day_slot in day_slots %}#}
{#                    <option {% if booking.date == day_slot %}selected{% endif %} value="{{ day_slot|date('Y-m-d H:i') }}">{{ day_slot|date('H:i') }}</option>#}
{#                {% endfor %}#}
{#            </select>#}
{#        </div>#}

    </div>
    <div class="grid grid-cols-1 sm:grid-cols-2 gap-x-8">
        {{ form_row(form.pet) }}
        {{ form_row(form.customer) }}
    </div>
    <div class="grid grid-cols-1 sm:grid-cols-2 gap-x-8">
        {{ form_row(form.estimatedDuration) }}
        {{ form_row(form.estimatedPrice) }}
    </div>
    <div class="grid grid-cols-1 gap-x-8">
        {{ form_row(form.notes) }}
    </div>
</div>

<div class="p-6 flex items-center justify-end border-t rounded-b-md bg-slate-50 relative">
    <button type="button" class="px-6 py-2.5 mr-3 bg-transparent font-medium text-xs leading-tight uppercase hover:text-blue-700 focus:text-blue-700 focus:outline-none focus:ring-0 transition duration-150 ease-in-out"
            onclick="$('.close-modal').trigger('click');">
        Cancelar
    </button>
    <button type="submit" id="send-form-button"
{#            {% if mode == 'add' %}disabled{% endif %}#}
            class="px-6 py-2.5 text-white font-medium text-xs leading-tight uppercase rounded shadow-md hover:shadow-lg focus:shadow-lg focus:outline-none focus:ring-0 active:bg-blue-700 active:shadow-lg transition duration-150 ease-in-out
{#                  {% if mode == 'add' %}bg-blue-300 hover:bg-blue-300 focus:bg-blue-300 pointer-events-none{% else %}bg-blue-500 hover:bg-blue-600 focus:bg-blue-600{% endif %}#}
                  ">
        {{ button_label|default('Guardar cita') }}
    </button>
</div>
{{ form_end(form) }}

<style>
    .select2-container--default .select2-selection--single {
        border-color: #d1d5db;
        height: 34px;
    }
    .select2-container--default .select2-selection--single .select2-selection__rendered {
        line-height: 32px;
        padding: 0 12px;
    }
    .select2-container--default .select2-selection--single .select2-selection__arrow {
        height: 34px;
        position: absolute;
        right: 10px;
        top: 1px;
        width: 20px;
    }
    .select2-container--default .select2-search--dropdown .select2-search__field {
        border-color: #d1d5db;
        border-radius: 0.25rem;
        font-size: 0.875rem;
        padding: 4px 8px;
    }
    .select2-search--dropdown {
        display: block;
        padding: 8px;
    }
    .select2-dropdown {
        border-color: #d1d5db;
        padding: 0;
        font-size: 0.875rem;
    }
    .select2-results__options {
        border-top: 1px solid #d1d5db;
        list-style: none;
        margin: 0;
        padding: 0;
    }
    .select2-container--default .select2-results__option[aria-selected=true] {
        background-color: #d1d5db;
        border-bottom: 1px solid #d1d5db;
        color: #1a202c;
    }
</style>

<script>
    $(document).ready(function() {
        $('#booking_estimatedDuration').select2();
        $('#booking_pet').select2();
        // $('#booking_pet').select2();
        $('#booking_pet').next('.select2-container').addClass('w-full p-0 border-0 text-sm font-normal text-slate-700 bg-white bg-clip-padding bg-no-repeat border border-solid border-slate-300 rounded transition ease-in-out m-0 focus:text-slate-700 focus:bg-white focus:border-blue-500 focus:outline-none placeholder-slate-400');
        // $('#booking_pet').next('.select2-container--default.select2-selection--single').addClass('border border-solid border-slate-300 rounded');
        // $('#booking_pet').next('.select2-dropdown').addClass('form-select appearance-none block w-full px-3 py-1.5 text-sm font-normal text-slate-700 bg-white bg-clip-padding bg-no-repeat border border-solid border-slate-300 rounded transition ease-in-out m-0 focus:text-slate-700 focus:bg-white focus:border-blue-500 focus:outline-none placeholder-slate-400');
        $('#booking_pet').next('.select2-container').find('.select2-selection').css('height: 42px');
        $('#booking_customer').select2();

    });
</script>