<div class="px-8 pb-3 -mx-8 flex items-center justify-between space-x-2 xl:space-x-4">
    <div class="flex-initial">
        <a href="{{ path('admin_booking', {'view': view, 'day': 'now'}) }}"
           class="px-3 xl:px-4 py-2 xl:py-2.5 rounded text-xs xl:text-sm bg-blue-500 text-white shadow-md hover:bg-blue-600 hover:shadow-lg focus:bg-blue-600 focus:shadow-lg focus:outline-none focus:ring-0 active:bg-blue-700 active:shadow-lg transition duration-150 ease-in-out"
        >
            <i class="fa-solid fa-plus mr-1.5"></i><span>Cita</span>
        </a>
    </div>
    <div class="flex-initial">
        <a href="{{ path('admin_booking', {'view': view, 'day': 'now'}) }}"
           class="border border-gray-300 px-3 xl:px-4 py-2 xl:py-2.5 rounded text-xs xl:text-sm hover:bg-slate-50 focus:bg-slate-50 focus:outline-none"
        >
            Hoy
        </a>
    </div>

    <div class="flex-initial flex items-center">
        <a href="{{ path('admin_booking', {'view': view, 'day': day|date_modify('-1 ' ~ view)|date('Y-m-d')}) }}"
           class="text-xs lg:text-sm xl:text-base font-medium focus:outline-none focus:ring-2 focus:ring-offset-2 w-8 h-8 flex items-center justify-center rounded-full focus:ring-gray-200 focus:bg-gray-100 hover:bg-gray-100 bg-transparent">
            <i class="fa-solid fa-chevron-left"></i>
        </a>
        <a href="{{ path('admin_booking', {'view': view, 'day': day|date_modify('+1 ' ~ view)|date('Y-m-d')}) }}"
           class="text-xs lg:text-sm xl:text-base font-medium focus:outline-none focus:ring-2 focus:ring-offset-2 w-8 h-8 flex items-center justify-center rounded-full focus:ring-gray-200 focus:bg-gray-100 hover:bg-gray-100 bg-transparent">
            <i class="fa-solid fa-chevron-right"></i>
        </a>
    </div>

    <div class="flex-1">
        <p class="text-base lg:text-lg xl:text-xl">
            <span class="hidden lg:inline-block mr-1.5">{{ day|date('l')|trans }},</span><span class="">{{ day|date('d')}} de {{ day|date('F')|trans }} de {{ day|date('Y') }}</span>
        </p>
    </div>
</div>