<thead class="h-10 px-3 bg-neutral-100 border-b border-slate-300">
    <th class="md:hidden py-1.5 text-left font-semibold whitespace-nowrap"></th>
    <th class="w-12 py-1.5 px-3 text-left font-semibold whitespace-nowrap">
        {{ knp_pagination_sortable(sales, 'Id', 'id') }}
    </th>
    <th class="w-36 py-1.5 px-4 text-left font-semibold whitespace-nowrap">
        {{ knp_pagination_sortable(sales, 'Fecha', 'dateAdd') }}
    </th>
    <th class="py-1.5 px-4 text-left font-semibold whitespace-nowrap">
        {{ knp_pagination_sortable(sales, 'Mascota', 'pet') }}
    </th>
    <th class="py-1.5 px-4 text-left font-semibold whitespace-nowrap">
        {{ knp_pagination_sortable(sales, 'Cliente', 'customer') }}
    </th>
    <th class="w-36 py-1.5 px-4 text-left font-semibold whitespace-nowrap">
        {{ knp_pagination_sortable(sales, 'Descuentos', 'totalDiscounts') }}
    </th>
    <th class="w-36 py-1.5 px-4 text-left font-semibold whitespace-nowrap">
        {{ knp_pagination_sortable(sales, 'IVA', 'totalTaxes') }}
    </th>
    <th class="w-36 py-1.5 px-4 text-left font-semibold whitespace-nowrap">
        {{ knp_pagination_sortable(sales, 'Total', 'total') }}
    </th>
    <th class="w-36 py-1.5 px-4 text-left font-semibold whitespace-nowrap uppercase text-xs">
        Pago
    </th>
    <th class="hidden md:block w-auto py-1.5 px-4 text-left font-semibold whitespace-nowrap uppercase text-xs"></th>
</thead>