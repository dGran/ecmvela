<header id="header" class="mb-6 -mx-8 px-8 border-b">
    <div class="md:mb-6 max-w-4xl">
        <p class="mb-1 md:mb-2 text-xs md:text-sm font-semibold text-blue-500">{{ this.section }}</p>
        <h1 class="text-3xl md:text-4xl font-extrabold text-slate-900 tracking-tight">{{ this.title }}</h1>
        <p class="mt-2 md:mt-4 text-base md:text-lg text-slate-700">{{ this.description }}</p>
    </div>
</header>
