<input type="{{ this.type }}" name="{{ this.name }}" id="{{ this.id }}"
       class="text-{{ this.textSize }} bg-white text-neutral-500 px-2.5 py-1.5 rounded border border-gray-200 focus:border-red-500 ring-transparent focus:outline-none"
>